import { GameResultPipe } from './game-result.pipe';

describe('GameResultPipe', () => {
  it('create an instance', () => {
    const pipe = new GameResultPipe();
    expect(pipe).toBeTruthy();
  });
});
