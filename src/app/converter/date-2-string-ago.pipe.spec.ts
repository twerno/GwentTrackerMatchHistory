import { Date2StringAgoPipe } from './date-2-string-ago.pipe';

describe('Date2StringAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new Date2StringAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
