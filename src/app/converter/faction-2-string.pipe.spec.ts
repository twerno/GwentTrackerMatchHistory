import { Faction2StringPipe } from './faction-2-string.pipe';

describe('Faction2StringPipe', () => {
  it('create an instance', () => {
    const pipe = new Faction2StringPipe();
    expect(pipe).toBeTruthy();
  });
});
