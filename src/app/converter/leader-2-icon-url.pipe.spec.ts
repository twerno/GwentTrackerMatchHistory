import { Leader2IconUrlPipe } from './leader-2-icon-url.pipe';

describe('Leader2IconUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new Leader2IconUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
