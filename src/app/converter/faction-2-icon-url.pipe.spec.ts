import { Faction2IconUrlPipe } from './faction-2-icon-url.pipe';

describe('Faction2IconUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new Faction2IconUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
